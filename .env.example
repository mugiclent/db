# Copy this file to .env for local development.
# In production these values come from GitHub Actions secrets — never commit .env.

POSTGRES_USER=katisha
POSTGRES_PASSWORD=change_me
POSTGRES_DB=katisha

# Service account passwords — set by the deploy pipeline in production.
# For local dev, set these to any non-empty value.
SEAWEED_DB_PASSWORD=change_me
INFISICAL_DB_PASSWORD=change_me
PGBOUNCER_AUTH_PASSWORD=change_me
NOTIFICATION_DB_PASSWORD=change_me
AUDIT_DB_PASSWORD=change_me
