# Local env file — values come from GitHub Actions secrets in production
.env

# GitHub Actions secrets — never commit filled values
actions.env

# Local dev script — not needed in CI
dev-up.sh

# Local docker-compose override — contains plaintext dev passwords
docker-compose.override.yml
